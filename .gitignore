.DS_Store

# Never commit real secrets — only *.example.json templates belong in config/
config/auth.json
config/web-search.json
config/settings.local.json
config/*.real.json

# Local pi settings (mirror of ~/.pi/agent/settings.json) — never commit
settings.json
config/settings.json
